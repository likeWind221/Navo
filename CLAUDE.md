# CLAUDE.md — SkillWorld 工作约定

## 项目一句话
从 0 手撕一个基于 DAG 的自主规划 Agent 系统（规划 → 执行 → 验证 → 再规划）；技能学习是它的第一个应用。系统核心不绑定学习域。

## 权威文档
- **PRD（0 号文档）：** `_docs/00-skillworld-prd.md` — 系统核心定义
- **计划（单一事实源）：** `_docs/plan.md` — 步骤分 Phase 0~5；执行任何开发前先查对应步骤的目标与完成标准
- **文档索引：** `_docs/index.md`

## Step 执行流程（必须遵守）

- 每个计划 Step（包括为修正既有设计而新增的子 Step）开始实现前，必须先按以下四部分向用户介绍，并等待确认：
  1. **该步骤是什么**：说明本 Step 的范围和交付物。
  2. **解决什么问题 / 起什么作用**：说明它服务的上层能力和缺失它会产生的问题。
  3. **打算怎么做**：从架构和功能层面粗略说明目标文件、模块职责、核心接口、主要调用流程和明确不做的内容；开始前不展开到逐行实现、局部辅助函数或完整类型字段。
  4. **为什么这样设计**：解释职责边界、依赖方向、关键取舍及替代方案；说明参考 DeepSeek Harness 的对应设计，以及当前方案与 Harness 的明确差异和保留差异的原因。
- 每个子 Step 的设计应优先靠近 DeepSeek Harness 已验证的职责划分、协议形态、生命周期和错误/取消语义；设计前先只读检查对应 Harness 源码。不得机械照抄：受当前 PRD、阶段范围或简化目标影响而不采用的能力，必须在步骤介绍和开发记录中明确说明。
- 未经用户确认，不实现该 Step，也不提前创建或修改后续 Step 的文件；用户明确要求连续执行时除外。
- 每个子 Step 完成开发后，必须从实际代码设计层面向用户介绍要点，包括：代码在整体架构中的位置、主要类型/接口与调用流程、关键辅助机制、Abort/错误/生命周期等边界处理、相对 Harness 的一致点与差异点，以及实际执行的验证；不能只罗列修改文件和测试结果。

## 代码组织约束（必须遵守）

- 类型协议和模块声明按“先整体、后局部”组织：先给出模块对外的最高层联合类型、入口类型或主要 API，再展开其公共支撑抽象、状态类型和具体变体；允许使用 TypeScript 类型前向引用。
- 不为满足声明顺序重复类型定义；具体实现细节仍应紧邻所属的抽象分组。
- `src/`、`tests/` 及其他目录下的手写代码文件总行数不得超过 300 行；接近上限时应按职责、生命周期或测试行为主动拆分，不得通过压缩格式或删除必要说明规避限制。generated、vendor、lockfile 等非手写文件豁免。
- 文件拆分必须保持清晰的单向依赖和职责边界，不得仅按行号机械切割；共享测试设施应提取为 helper，避免通过复制代码满足行数限制。
- 每个 Plugin/领域模块必须按职责解耦，公共协议、错误定义、核心生命周期、独立机制和测试实现不得长期堆积在同一文件；拆分应保持单向依赖和清晰边界，跨 Plugin 只依赖公共协议或 Service API，不依赖私有辅助实现。
- 不强制所有 Plugin 采用相同的文件结构；不得为满足目录外观创建空文件或无语义转发层，Mock、fixture 和 test helper 不进入生产默认组合。
- 每个 Plugin/领域模块的公开可抛出错误不得汇总到全局 errors 文件；可序列化的 `Failure`、`Result` 不得与可抛出的 `Error` class 混放。

## 文档管理约束（必须遵守）
1. **所有 markdown 文档只允许放在 `_docs/`**，项目根目录不放任何 `.md`（本文件除外）。
2. **编号命名：** `_docs/NN-Name.md`，数字为编号（00 = PRD），文件名小写中划线；`plan.md`、`index.md` 为两个例外（无编号前缀）。
3. **文档编号回收**：删除文档后其编号需要重新接上前面的文档序号。
4. **每次完成一个计划步骤（plan.md 中的一行）后，必须同时做三件事：**
   - 写一个对应的**简单开发记录文档** `_docs/NN-devlog-<步骤短名>.md`（新编号，紧随已有最大值），内容保持简短：`做了什么 / 关键决策 / 坑与发现 / 下一步`；
   - 在 `_docs/index.md` 登记该文档一行；
   - 在 `_docs/plan.md` 中把对应步骤状态从 `⬜` 改为 `✅`（部分完成用 `🔄`）。
   - 这三件事缺一件，视为该步骤未完成。
   - 可以根据用户需求，进行合并步骤文档。
5. **调研/探索步骤同样要留痕**：调研结论写进对应的开发记录文档，不只在对话里说。
6. **文档语言：** 中文；代码标识符、专有名词用英文。文件名也遵循。

