# CLAUDE.md — SkillWorld 工作约定

## 项目一句话
从 0 手撕一个基于 DAG 的自主规划 Agent 系统（规划 → 执行 → 验证 → 再规划）；技能学习是它的第一个应用。系统核心不绑定学习域。

## 权威文档
- **PRD（0 号文档）：** `_docs/00-skillworld-prd.md` — 系统核心定义
- **后端计划（后端单一事实源）：** `_docs/backend-plan.md` — Agent 内核、领域服务与后端测试
- **前端计划（前端单一事实源）：** `_docs/frontend-plan.md` — Electron、React 与桌面交互
- **文档索引：** `_docs/index.md`

## 前后端 Agent 隔离（必须遵守）

### 所有权边界

- 后端 Agent 只写 `_docs/backend-plan.md`、`src/**`、`tests/**`、`scripts/**`、根目录 `package.json`、根目录 `pnpm-lock.yaml` 及明确分配给后端的开发记录，不得修改 `frontend/**`。
- 前端 Agent 只写 `_docs/frontend-plan.md`、`frontend/**` 及明确分配给前端的开发记录，不得修改 `src/**`、`tests/**`、`scripts/**`、根目录 `package.json` 或根目录 `pnpm-lock.yaml`。
- 前端依赖只记录在 `frontend/package.json` 与 `frontend/pnpm-lock.yaml`；后端依赖只记录在根包清单与根锁文件。不得把两个工程改成共享 lockfile，也不得从 Renderer 直接导入根目录 `src/**`。
- `_docs/00-skillworld-prd.md`、`_docs/index.md`、`CLAUDE.md`、`.gitignore` 以及未来共享的 IPC/RPC 契约属于共享控制面；两个 Agent 不得并发修改，必须先指定唯一写入者并串行提交。
- 跨端需求先分别写入所属计划；只有需要稳定数据交换时才新增共享契约 Step。共享契约必须明确版本、运行时校验、错误、取消和兼容语义，前端不得依赖后端私有 Store、Cordis Context 或内部事件实现。

### 并行开发规则

- 两个 Agent 开始前分别读取 PRD、自己的计划和 `git status`，不得顺手实现对方计划中的 Step。
- 一方发现必须修改对方所有文件时，停止实现并提出交接，不以“顺便修复”为由跨界写入。
- 共享控制面变更、跨端契约落地、合并与完整验收必须串行执行；其他位于所有权目录内的工作可以并行。
- 每个 Agent 提交前检查 `git diff --name-only`；出现越界文件时先拆分或回退，不把跨端改动混入同一提交。

## Step 执行流程（必须遵守）

- 用户说“进入/开始某 Step”时，必须先用**一小段精简说明**介绍并等待确认，再开始实现；说明应覆盖：解决的功能问题、需要创建/修改/删除的文件、各文件承担的主要功能和明确不做的内容。只有当核心算法、依赖方向、错误/取消语义或关键取舍会显著影响设计时，才额外简述；开始前不展开逐行实现、局部辅助函数或完整类型字段。用户直接点名说“完成/实现某 Step”时，视为已经确认，可以在简短说明后直接实现，不再额外等待。
- 上述规则同样适用于为修正既有设计而新增的子 Step。
- 后端子 Step 的设计应优先靠近 DeepSeek Harness 已验证的职责划分、协议形态、生命周期和错误/取消语义；设计前先只读检查对应 Harness 源码。不得机械照抄：受当前 PRD、阶段范围或简化目标影响而不采用的能力，必须在步骤介绍或开发记录中明确说明。纯前端 Step 不强制调研 Harness；涉及 Agent 协议或取消语义时仍须检查。
- 未经用户确认，不实现该 Step，也不提前创建或修改后续 Step 的文件；用户明确要求连续执行时除外。

## 代码完成后的交付说明（必须执行）

### 目的

用户需要通过完成回复理解本次代码的模块划分、具体实现和设计原理，并能据此快速 review。完成回复是代码导读，不得只有文件清单、功能概括、测试结果或开发记录链接。

### 执行时机与防遗漏

- 开始任务时，读取本文件和当前所属计划。
- 上下文压缩或会话恢复后，在继续修改前重新读取本节及当前 Step 状态。
- 完成代码、准备最终回复前，重新读取本节，并核对本次实际 diff 和新增文件；不要仅凭对话记忆组织说明。
- 交接记录必须保留：当前 Step、用户已授权范围、本次修改文件、已完成验证、未落实的契约，以及是否已交付代码导读。
- 以下自检由 Agent 自行完成，不要求用户重复提醒或额外确认。

### 最终回复结构

#### 1. 模块地图

用简洁架构图说明本次涉及哪些模块、各自职责及依赖或调用方向。标明新增、修改和复用的部分；英文名称首次出现时附中文释义。区分当前已经接入的模块与仅为后续定义的协议。

#### 2. 按模块导读代码

按照模块地图的顺序介绍，每个模块提供可点击的文件或行号链接。

- 先说明该模块解决什么问题、为什么由它负责。
- 对新增或实质修改的生产函数（包括重要 helper），说明：输入与输出/副作用、具体处理步骤、调用的辅助函数，以及与本次实现有关的错误、取消或状态变化。
- 简单函数可简短说明；复杂函数应展开关键算法与不变量。不限制固定句数，不逐行翻译代码，不重复解释未修改的代码。
- 对类型、Schema 和配置模块，解释关键字段的关系、联合类型如何区分情况、默认值/边界/分页语义，以及设计原因。明确区分类型约束、运行时已校验规则和仍待后续落实的契约。
- 对不直观或容易误解的行为，给出一个简短输入→处理→结果示例。
- 关键取舍放在对应模块内解释，并指出最值得 review 的位置；不泛泛复述“安全、可扩展、职责清晰”等结论。

#### 3. 验证与实际边界

说明实际运行了哪些检查、检查证明什么，以及哪些行为尚未验证或尚未实现。不得把类型检查通过等同于运行时行为正确。

参考 DeepSeek Harness 时，指出具体源码路径、采用的机制和与本项目的差异，不能只写“已对齐 Harness”。

### 按任务类型调整

- 前端：解释组件/Hook 分工、props/state、事件触发后的状态与 UI 变化；纯样式变更解释布局、作用域和交互规则，不虚构函数。
- 测试：围绕场景、输入、关键断言和覆盖边界说明，不逐个介绍 fixture、mock 或测试框架调用。
- 纯文档：说明内容或组织方式如何变化及引用检查结果，不套用代码模块导读。
- 用户明确要求简短回复时可压缩，但仍保留实现机制和实际边界。

### 交付前自检

- 用户能否知道本次写了哪些模块、它们如何配合？
- 用户能否理解关键代码具体怎样实现，而不只是知道它的名字？
- 关键设计是否解释了原因，并有代码位置可供核对？
- 是否明确指出“已实现”和“仅定义契约”的区别？
- 是否遗漏本次重要函数、分支、状态变化或错误路径？

若有缺项，补齐最终回复后再交付。不要输出空泛的“已遵守规则”声明。

## 代码组织约束（必须遵守）

- 类型协议和模块声明按“先整体、后局部”组织：先给出模块对外的最高层联合类型、入口类型或主要 API，再展开其公共支撑抽象、状态类型和具体变体；允许使用 TypeScript 类型前向引用。
- 不为满足声明顺序重复类型定义；具体实现细节仍应紧邻所属的抽象分组。
- **职责与状态所有权优先于行数。** 协议、状态机、安全边界、进程入口和 Plugin 装配可以独立成文件，即使文件少于 40 行；不得为了消灭短文件而合并不同生命周期或依赖方向。
- **合并候选必须具有高变更耦合。** 两段代码长期一起修改、只有单一生产消费者，且没有独立公共契约或测试价值时，优先放在同一文件；仅共享主题或目录不构成合并理由。
- **有状态代码按所有者拆分。** 队列、缓存、AbortController、资源句柄、重试和生命周期清理由拥有该状态的模块封装；两个独立状态机不应因复用少量 helper 而放入同一文件。
- 手写文件少于 40 行时复核其独立边界和消费者数量；超过 250 行时复核是否包含多个状态机或变化原因；超过 300 行必须记录保留理由或拆分。阈值用于触发审查，不得通过压缩格式或删除必要说明规避；generated、vendor、lockfile 等非手写文件豁免。
- 文件名必须表达模块内的具体角色；同一模块的子实现用目录限定归属，例如 `adapters/qwen.ts` 与 `adapters/qwen/sse.ts`，不用连字符拼接模块名和职责。
- 除包级公共出口外，不新增只做 re-export 的微型 `index.ts`；内部代码优先直接导入所有者文件，避免 barrel 隐藏依赖方向或制造循环依赖。
- 可复用基础设施应提取为 helper，但单次使用、短小且紧贴调用者的实现默认就地保留；不得为目录对称创建空文件、无语义转发层或一函数一文件。
- 每个 Plugin/领域模块按职责内聚；公共协议、错误定义、业务规则、生命周期和状态编排需要拆分时建立单向依赖，跨 Plugin 只依赖公共协议或 Service API，不依赖私有辅助实现。Mock、fixture 和 test helper 不进入生产默认组合。
- 分文件重构必须在同一改动中更新生产导入、测试导入、计划/架构文档和公共出口，并用 `rg` 确认旧路径无残留；纯结构重构必须保持业务行为与既有测试断言不变。
- 每个 Plugin/领域模块的公开可抛出错误不得汇总到全局 errors 文件；可序列化的 `Failure`、`Result` 不得与可抛出的 `Error` class 混放。

## 文档管理约束（必须遵守）
1. **所有 markdown 文档只允许放在 `_docs/`**，项目根目录不放任何 `.md`（本文件除外）。
2. **编号命名：** `_docs/NN-Name.md`，数字为编号（00 = PRD），文件名小写中划线；`backend-plan.md`、`frontend-plan.md`、`index.md` 为例外（无编号前缀）。
3. **文档编号回收**：删除文档后其编号需要重新接上前面的文档序号；并行开发时，开发记录编号由共享控制面的唯一写入者分配，两个 Agent 不得自行占用同一编号。
4. **每次完成任一计划中的一个步骤后，必须同时做三件事：**
   - 写一个对应的**简单开发记录文档** `_docs/NN-devlog-<步骤短名>.md`（新编号，紧随已有最大值），内容保持简短：`做了什么 / 关键决策 / 坑与发现 / 下一步`；
   - 由共享控制面的唯一写入者在 `_docs/index.md` 登记该文档一行；
   - 在所属的 `_docs/backend-plan.md` 或 `_docs/frontend-plan.md` 中把对应步骤状态从 `⬜` 改为 `✅`（部分完成用 `🔄`）。
   - 这三件事缺一件，视为该步骤未完成。
   - 可以根据用户需求，进行合并步骤文档。
5. **调研/探索步骤同样要留痕**：调研结论写进对应的开发记录文档，不只在对话里说。
6. **文档语言：** 中文；代码标识符、专有名词用英文。文件名也遵循。

## 用户偏好：源码命名与模块目录

- 用户偏好用目录表达模块归属、用简短单词表达文件职责；手写源码、测试与脚本不采用连字符组合词，也不改成同样冗长的驼峰文件名。
- 普通文件用小写单词（如 `stream.ts`、`validation.ts`、`process.ts`）；React 组件文件用单词 PascalCase（如 `Chat.tsx`、`Shell.tsx`）。
- 顶层契约或模块入口保留在父目录，所属实现与测试收纳到同名小写目录，例如 `content.ts` + `content/`、`Chat.tsx` + `chat/`。
- 按职责、依赖和状态所有权分目录；单文件模块不强制建目录，不为目录对称添加空层或只做转发的 index.ts。
- 文件改名默认保持导出符号及运行协议不变；同步更新静态／动态导入、测试发现、脚本入口、构建配置与当前架构文档，并验证旧路径无运行引用。
- `.spec.ts`、`.module.css`、`.d.ts` 和工具约定配置名保留；该偏好不要求重命名编号文档、依赖、第三方代码、锁文件或历史产物。
- Electron 与 Renderer 共用的桌面契约放在 `frontend/shared/`，不得让主进程依赖 Renderer 内部模块。
