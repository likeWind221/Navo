# 68：Bug 跟踪机制与 F9.8 已知问题交接

## 做了什么

- 按用户要求新增 [bug-plan.md](bug-plan.md)，登记 BUG-001 至 BUG-004：续接资源发现、文件注册失败、状态报告不一致、失败证据留存不足。全部未勾选，本次仅登记与整理诊断事实，未实施修复。
- 在 [AGENTS.md](../AGENTS.md) 增加 Bug 文档职责、稳定 ID、原生任务勾选框、独立修复笔记、索引登记、关闭与复发规则，以及修复分支和验证要求；共享文档由本任务串行维护。
- 后续笔记采用 `NN-bugfix-bug-NNN-<短名>.md`，与 Bug 表和索引互相链接；排查尚未解决时也需写清未修复，不允许先勾选再补验证。

## 关键决策

- Bug 表记录现象与关闭条件，具体原因以实际证据为准。修正前文过强的归因：文件来源不可用不必然是模型猜错路径，realpath/stat 的 I/O 失败也会生成该错误；现有输出没有失败 path 或底层原因，故保持待定位。
- 不把共享 Workspace 重新发布行为直接视为 ACL 缺陷，不预先决定用文件枚举或白名单修复。先取得完整调用证据。
- 用户在知晓真实模型失败后明确要求“先把这个阶段进行 master 合并”。本次合并验收脚本与阶段文档，不把真实验收标为通过；F9.8 保留进行中说明，未修复项交由 Bug 表持续跟踪。该授权仅适用于本次已知问题，不豁免后续 CI 或本地检查。

```text
Observed failure -> bug-plan.md [ ] -> bugfix note
                                      |
                                      v
                                Fix + verification
                                      |
                                      v
                           bug-plan.md [x] + index

F9.8 delivery -> PR #30 -> master
                   |
                   +-> BUG-001..004 remain [ ]
```

## 坑与发现

- 现有 [F9.8 记录](67-devlog-f9.8-longterm-acceptance.md) 保留两次真实尝试的失败；本地 78 文件、457 项离线测试及 TypeScript 检查通过，提交 `73380c7` 的 CI 已通过。此次仅改文档，不重跑已通过且未变化的代码检查；文档引用与 diff 另行核对，合并前等待最新提交 CI。
- 当前没有生产代码改动、Bug 修复完成或真实模型新增成功样本。F9.9 共享契约与前端交接仍未开始。

## 下一步

- 完成本次授权的 PR #30 squash 合并；合并结果以 GitHub PR 记录为准。
- 按 Bug 表先补齐失败证据，再逐项定位、修复和复验。最终交付说明提供文档入口和合并结果，不将本次治理记录冒充修复笔记。
