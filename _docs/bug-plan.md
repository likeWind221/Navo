# Bug 跟踪与修复计划

此文档是 Bug 修复状态的单一事实源；功能阶段继续在 [后端计划](backend-plan.md) 和 [前端计划](frontend-plan.md) 管理。勾选表示修复验收完成，合并代码不等于修复 Bug。维护规则见 [AGENTS.md](../AGENTS.md)。

## 当前状态

2026-09-22：登记 4 项未关闭问题，尚未实施生产修复。用户明确要求将已发现问题转入此表后先合并 F9.8 阶段成果；本次允许带已知真实验收失败合并，不改变后续验证门禁，也不声称真实模型闭环已通过。阶段交付见 [PR #30](https://github.com/likeWind221/Navo/pull/30)。

- [ ] BUG-001：Node 续接时未读取已共享的新验证资源（待定位）
- [ ] BUG-002：验证节点发布文件时出现来源不可用错误（待定位）
- [ ] BUG-003：Agent 文字状态报告与当前领域状态不一致（待定位）
- [ ] BUG-004：真实验收的失败现场留存不足（待修复）

## 待修复表

优先级：P1 阻断目标闭环，P2 影响诊断或报告可信度。优先级不代表已确认生产代码缺陷。

| ID | 优先级 / 所属端 | 现象与影响 | 证据与归因状态 | 关闭条件 | 笔记 |
|---|---|---|---|---|---|
| BUG-001 | P1 / 后端 | synthesis 续接仍读取旧 evidence，报告缺少 validation，无法产出最终推荐 | 首次有效运行第 8 回合；Store 权限检查通过，但未留存该回合请求快照。上下文传递遗漏、模型漏读或旧历史干扰均待确认 | 留存该回合模型输入和调用结果，定位并处理原因；真实模型读取新 Resource 并形成有依据的结论，同时保持原 Session 和 Human Gate；笔记记录完整复验 | [发现记录](67-devlog-f9.8-longterm-acceptance.md)、[登记与排查边界](68-devlog-bug-tracking.md)；修复笔记待创建 |
| BUG-002 | P1 / 后端 | validation 注册文件时出现 `Resource source file is unavailable`；模型随后恢复，但严格验收在第 6 回合停止 | 第二次有效运行；未留存失败调用的 path 和底层 I/O 原因。该消息可来自 realpath/stat 的 I/O 失败，不能据此断言一定是模型猜错文件名 | 捕获实际参数及失败原因，明确输入文件交接与工具错误恢复的预期；处理已证实问题，真实场景能正确发布/读取所需文件；明确可恢复工具错误的验收标准，不能仅删断言以通过 | [发现记录](67-devlog-f9.8-longterm-acceptance.md)、[登记与排查边界](68-devlog-bug-tracking.md)；修复笔记待创建 |
| BUG-003 | P2 / 后端 | Main 在 synthesis 实际 idle 时仍报告 locked，误导用户下一步操作 | 首次有效运行的交接回复；属于可观察的文本与事实不一致，具体提示、历史或查询使用原因待确认。回合内 working 与回合后 idle 的正常转换不算此问题 | 关键状态报告基于当前可信事实；以实际状态转换场景验证报告一致，无法确认时表达未知；保留真实复验文本与对应快照 | [发现记录](67-devlog-f9.8-longterm-acceptance.md)、[登记与排查边界](68-devlog-bug-tracking.md)；修复笔记待创建 |
| BUG-004 | P2 / 后端验收脚本 | 失败输出仅含工具名称与错误摘要，缺少具体参数和关联上下文；应用释放后不能还原现场，妨碍 BUG-001/002 定位 | `scripts/longterm.ts` 已增加请求资源 ID 摘要，但首次失败未有此摘要；未持久化失败所需事件。此诊断缺口已确认 | 失败时留下足以关联 Turn、输入资源元数据、工具参数、结果和状态的脱敏证据；复现后可区分上下文、模型选择与 I/O 错误，不泄露凭据或记录无关数据 | [登记与排查边界](68-devlog-bug-tracking.md)；修复笔记待创建 |

## 修复记录规则

首次修复或进一步实质排查时创建下一可用编号的 `NN-bugfix-bug-NNN-<短名>.md`，并在上表与索引建立链接；同一问题追加至已有笔记。每项写清复现、根因证据、改动、实际验证、剩余边界和 PR/提交。完成并通过该项关闭条件后才将对应任务改为 `[x]`。

建议先处理 BUG-004，再定位 BUG-001/002；BUG-003 可在取得同回合事实后定位。当前没有已修复条目。

## 已识别但不列为已确认 Bug 的边界

- `synthesis` 是测试目标为“综合结论”的普通工作节点，不是特殊 Agent 类型。
- Resource ACL 控制已发布资源的读取；Project Workspace 是共享文件空间。能从用户 Workspace 重新发布文件不直接证明 ACL 被绕过，也不据此擅自增加节点文件白名单。
- “每回合不得发生任何工具错误”是当前真实验收脚本的严格判据，不等同于运行时不允许模型恢复；其业务适用性在 BUG-002 中一起明确。
